Dialog command list
available command are:
  #reserve [command] (load a reserve obj from ./reserve)
   commands are:
     map [X] [Y] [version]
  
  #jmp [line] (jump to [line(int)])

  #setpos;posx;posy

  #dialog;name;spriteset;sprite;text;spritepos
    available integrated names are:
      ??? - for a character thats not existing
      player - for the player name

  
